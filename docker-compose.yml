services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pixanova-preview-poc
    ports:
      - "3000:3000"
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
